# Auth0 Settings
AUTH0_DOMAIN=your-auth0-domain.auth0.com
AUTH0_AUDIENCE=your-auth0-audience

# Frontend URL for CORS
FRONTEND_URL=http://localhost:5173

# Stripe決済設定（⚠️ セキュリティ重要！⚠️）
# 本番環境では必ずテスト用キーではなく本番用キーを使用してください
# テスト環境用キー例（sk_test_ で始まる）
STRIPE_SECRET_KEY=sk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# Webhook署名検証用シークレット（whsec_ で始まる）
STRIPE_WEBHOOK_SECRET=whsec_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ダミー認証用秘密鍵（本番では必ず変更）
DUMMY_JWT_SECRET=your-super-secret-key-for-dummy-auth

# AWS Settings (Optional - for local development)
AWS_REGION=ap-northeast-1
AWS_ACCESS_KEY_ID=your-access-key-id
AWS_SECRET_ACCESS_KEY=your-secret-access-key

# Serverless Offline Settings
IS_OFFLINE=true

# 🔒 セキュリティ注意事項 🔒
# 1. このファイルはサンプルです。実際の .env ファイルは絶対にGitにコミットしないでください
# 2. 本番環境では強固な秘密鍵を使用してください
# 3. 定期的にキーをローテーションしてください
# 4. 環境変数はサーバーの環境設定で管理してください